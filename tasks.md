1. Добавь обработку голосовых сообщений в Telegram для парсинга транзакций без поломки проекта, архитектурно правильно и чисто. Сразу пиши чистый код, как будто ты senior-разработчик.

Дополнительные задачи:
1. Добавить обработку голосовых в  парсинг транзакций и Jarvis-редактирование счетов.

2. Убрать возможность создавать пустые транзакции (нулевые по сумме). Например, я случайно написал в состояние парсинга транзакций текст "иди сюда", – а ИИ спарсил этот текст и создал транзакцию, с нулевой суммой, с тегом "Другое", с названием "Транзакция" и т.д., то есть пустая транзакция с 0,00$. Зачем? Как можно запретить обрабатывать транзакции, у которых отсутствует сумма в описании? Главное, ограничить так, чтобы не было багов, по типу необработки транзакции с голосового сообщения, если там будет транскрибация суммы не в цифрах, а в тексте ("девяносто девять долларов") и т.д. Это нужно исправить, а точнее улучшить, и улучшить так, чтобы не сломать текущие правила, которые хорошо работают, не сломать понимание ИИ в обработке транзакций и так далее. ТО есть текущие правила не менять, а просто добавить новое улучшение в обработку транзакций.

3. Улучшить парсинг транзакций по фото: я отправил скриншот с банка, где у меня есть расходы, которые выделены "-", например: "-23.99" и т.п., а так же у меня были доходы, они выделены в зеленый цвет обводки, и написаны без знака минус "-", то есть логически бы подумать, что это доход. Но ИИ почему-то спарсил эти транзакции с типом "Расход". Это нужно исправить, а точнее улучшить, и улучшить так, чтобы не сломать текущие правила, которые хорошо работают, не сломать понимание ИИ в обработке транзакций и так далее. ТО есть текущие правила не менять, а просто добавить новое улучшение в обработку транзакций, мол зелёный цвет транзакций обрабатывать как доход, и обращать внимание на знаки "-", без знаков, и знаки "+", а так же слова "Прибыль", "Доход", "Получение", "Receive" и тому подобное, которые будут означать тип транзакции "Доход", и аналогично "send" для "перевод", и для расхода "-" или другие релевантные данные. 

4. Часовой пояс пользователя при регистрации в боте (первая отправка /start) по умолчанию должен быть UTC+2). 

5. Сейчас если включить, например, парсинг часового пояса в панели "Настройки", и потом перейти на парсинг транзакций, то приоритетно ещё будет работать парсинг часового пояса, а парсинг транзакций будет пока игнорировать входящие сообщения пользователя. Но это неправильно. Любой новый активированный парсинг (состояния парсинга) должен отключать предыдущий, заканчивать его. То есть нужно внести исправление: новое состояние парсинга отключает предыдущие состояния парсинга. Это касается Jarvis-редактирования, панели редактирования транзакций (название, сумма и т.д.), парсинг транзакций, парсинг добавления категорий, тегов и так далее. 

6. После того, как в Jarvis-редактировании пользователь обновляет сумму актива, пользователю предлагается создать операцию для этого изменения –> пользователь создаёт операцию (транзакцию), и просто в конце получает просмотр созданной транзакции (ранее называли "предпросмотр"). Но, теперь, после просмотра созданной транзакции, нужно добавить автоматическую отправку главной панели (/start), сразу же после "предпросмотра". 

6. Пользователь только что зарегистрировался (/start). У пользователя нету счетов (за исключением системного, скрытого "Вне Wallet"). У пользователя пустой аккаунт. Пользователь сразу же идёт создавать транзакцию расхода на какую-то сумму. Транзакция создается, счёт определяется "Вне Wallet", в отображении предпросмотра как прочерк "–", но в скобках как "(Основной)", а в отображении списка транзакций – поле "Счёт": Вне Wallet. 

Это категорически неправильно и недопустимо. Счёт "Вне Wallet" должен использоваться только для типов транзакции "Перевод". Его нельзя использовать для "Расходы" или "Доходы". Второе: пользователь не может создать транзакцию, если у пользователя нету счетов (счёт "Вне Wallet" никогда не должен считаться в счета, он системный и скрытый) – ему нужно запретить "➕ Добавить транзакцию", то есть при нажатии на эту кнопку из Reply Keyboard или Inline Keyboard – сразу сообщения-ошибка с кнопкой "Закрыть", что у вас отсутствуют счета, мол добавьте их во вкладке "Счета".
7. Сейчас, если создать транзакцию через ИИ-парсинг, присылается сообщение предпросмотра созданной транзакции и затем рендер /start-сообщения. Если я начну взаимодействовать с этим /start-сообщением, например зайду в список транзакций, то сообщение-предпросмотра созданной транзакции, которое было над сообщением /start – удаляется. Так быть не должно, не нужно удалять это сообщение предпросмотра созданной транзакции. 

8. В панель редактирования в details_transaction а так же в вид предпросмотра созданной транзакции (просмотр), для сообщений-кнопок "Категории" и "Теги" нужно добавить под ряд кнопок пагинации, отдельным рядом, сместив кнопку "Назад" на ряд ниже, добавить кнопку "Создать новую категорию" и "Создать новый тег" (отдельно для каждой 1). При нажатии на эти кнопки, нужно вызывать состояние парсинга, которое будет парсить текстовое значение пользователя для новой категории или нового тега, и добавлять их в аккаунт пользователя. После успешного добавления, нужно стирать сообщение пользователя, стирать сообщение-подсказку парсинга и обновлять предпросмотр или details_transaction через edit_message с новой добавленной категорией/тегом в нужном поле Категория/Тег. 

9. Для кнопки "Счёт" в панели редактирования транзакций для транзакций с типом "Расход" и "Доход", нужно убрать отображение счёта "Вне Wallet" и вообще запретить его выбор именно для типов "Доход" и "Расход" как пользователю, так и ИИ-LLM. 
А так же, для типа транзакции "Перевод", счёт "Вне Wallet" может быть выбран только в одном поле: либо в "Счёт", либо в "На счёт". Но в двух полях его выбор нужно запретить, и всегда, при смене "Счёт" и "На счёт" на "Вне Wallet" нужно отображать в другом поле (параллельном – "Счёт" <–> "На счёт") основной счёт пользователя. 

10. В аналитике, для "Топ доходов:", "Топ расходов:", вместо тега в форматировании "Цитата" (<blockquote>) нужно отображать название транзакции, а не тег (остальные данные нужно сохранить). 
Для "Крупнейший перевод" – сейчас там отображается тег, если тега нет – название. Но нужно оставить только отображение названия. 
Так же нужно улучшить парсинг входящего сообщения пользователя для поля "Название" (description) в создании транзакций. Это значит, что нужно всего лишь улучшить понимание и распознавание названия: например, я написал текст "перевод папе 150 евро", а ИИ спарсил эту транзакцию с названием "Перевод", хотя логичнее было бы "Папе", ведь по типу транзакции и так понятно, что это "Перевод", это не нужно упоминать снова. Я же не упоминаю в транзакции с типом "Расход", что это "Расход на молоко", я просто напишу в название "Молоко". Так и тут: не "Перевод", а "Папе", ведь из типа транзакции и так понятно, что это перевод. И это касается не только этого случая – нужно улучшить LLM-парсинг транзакций на все общие случаи жизни для любых ситуаций, чтобы это работало безупречно. Но главное, не переборщить, чтобы названия не создавались какие-то плохие или не релевантные. Всего должно быть в меру, главное: точно и релевантно. Если информации не хватает – идти по дедукции и оставлять самое подходящее исходя из описания пользователя, или исходя из информации, данной на скриншоте (описание транзакции, название магазина, сумма списания – на эти детали обращать внимание для релевантного названия, т.е. вопрос: "Чем занимается этот магазин? - Он продает хоз. товары. – Так и запишу в название: "Хоз. товары"). 

11. Когда срабатывает курс валют, он записывает в базу данных и выдает в CSV файле сконвертированную сумму с большим кол-вом decimals. Нужно ограничить для fiat кол-во decimals: 2, а для crypto = до 18 decimals, в зависимости от самой криптовалюты и её сети. Поля "crypto | fiat" уже есть в БД, в таблице currencies для каждой валюты, и там уже проставлены значения.

Дополнительный список 2: 
1. Я создаю транзакцию через парсинг. После создания транзакции, бот отправляет просмотр созданной (или созданных) транзакции и затем сообщение /start. Проблема: сообщение /start отправляется без обновлённой информации касаемо созданной транзакции, то есть информация (математическая статистика по счетам и транзакциям) пользователя отправляется ещё старая, до создания транзакции(й) в просмотре. Как исправить: отправлять после создания транзакции новое, обновлённое, с актуальными статистическими данными в отображении сообщение /start.
2. При добавлении счетов через парсинг – названия слишком жестко нормализируются. Проблема: пользователь вводит входящий текст для создания счёта: "Abank: 2 eur, 0 usd, 0 uah", а LLM-парсинг счетов создает счёт с названием "Банк", вместо введенного пользователем "Abank". Ещё пример: пользователь вводит текст "Trust Wallet 2, TRX 21,7829", а ИИ-парсинг создает счёт с названием "Trust Wallet", буквально упуская "2", потому что посчитал её за опечатку. Как нужно исправить: нужно убрать жёсткую нормализацию именно и только для названий счетов и только в парсинге для счетов. То есть названия должны быть такими, какими их указывает пользователь. При этом, не нужно захватывать названия валют. Если ИИ-парсинг распознал валюту и её сумму – это значение он удаляет из предполагаемого текста для названия. Для названия остается весь текст, который не был добавлен в активы (не был распознан как валюта и сумма), и он нормализируется с заглавной буквы, без точек и запятых в конце. 
3. Заметил, что в парсинге счетов, когда ИИ отправляет предпросмотр добавления счетов, и если пользователь сохраняет эти счета из списка – тогда сообщение-подсказка о парсинге удаляется, входящий текст пользователя с информацией о счетах удаляется, и отправляется сообщение успех об успешном создании счёта, и старое сообщение списка счетов обновляется через edit_message. В чем проблема: бот удаляет входящее сообщение (текст с информацией о счетах) пользователя. Как исправить: не удалять входящий текст пользователя, который использовался для парсинга транзакций. 
4. В details_account и в предпросмотре создания счёта, где есть кнопки "Jarvis-редактирование" (для счетов) нужно добавить, в одну колонку с "Jarvis-редактирование" кнопку "Название" для редактирование названия. Из Jarvis-редактирования нужно убрать возможность менять название. Jarvis-редактирование должно менять только валюты и их суммы (кол-во) в тех правилах, в которых сейчас и меняет. То есть ты ничего не затрагиваешь лишнего, а только убираешь возможность менять название из Jarvis-редактирования, и переносишь эту возможность на новую кнопку – Название. Flow для названия: пользователь нажимает на кнопку "Название", отправляется новое сообщение-подсказка о активации изменения названия (через ИИ-парсинг), и полученное входящее сообщение пользователя ИИ (LLM) подставляет в поле "Название" для этого счёта. Должна быть нормализация названия с заглавной буквы. Этого достаточно. 

Затрагивай только функции, которые я упомянул. Пиши чистый код, словно ты senior-разработчик. Следуй правилам безопасности разработки.

Дополнительные задачи 3:
По запросу "вывел 100 еврро" почему Счёт: 💼 Paypal
На счёт: 💶 Наличные, и почему название "Наличные"? Проблема: не релевантные заполненные поля от ИИ-парсинга транзакции. Как должно было быть: Счёт: основной счёт пользователя, На счёт: Вне Wallet, название: "Вывод". Почему должно быть так? Потому что:
Во входящем сообщении пользователя:
1. не было названо, с какого и на какой счёт идёт перевод, сказано просто что было выведено, но откуда и куда – неизвестно, именно поэтому
2. если не указан счёт транзакции – тогда подставляется основной счёт пользователя это раз, второе не было указано куда сделан вывод – значит это тип "Перевод" с полем "На счёт:" Вне Wallet 
3. название "Вывод" подходит, потому что это максимально точно описывает входящий текст пользователя. Текущее название перевода "Наличные" вообще не подходит, и макисмально не релевантное, потому что входящий текст вообще нигде не упоминал, что это наличные и тому подобное 

Вопрос к тебе: как можно и нужно улучшить текущий парсинг транзакций, чтобы он не сломал уже существующие правила и реелвантное распознавание транзакций, но чтобы устранил вот эту текущую проблему, которая поможет закрыть множество других подобных запросов релевантно? То есть исправление, которое затронет не только этот конкретный запрос, а которое поможет закрыть множество других подобных этому запросов максимально релевантно?

1. Можно сделать, но нужно нормализовать данные, например если пользователь пишет "нал" – это подходит к счёту "Наличные" или "Cash" и тому подобное. Если пишет "моно" – так же подходит к Monobank и т.п. Это просто примеры, но кейсы неограничены. Сверять и нормализовать нужно исходя из реальных счетов пользователя в его аккаунте по их названиям. Только это должно быть по дедукции, чтобы например не было такого: пользователь пишет "купил моно колесо за 300 евро", а ИИ парсит это на счёт Monobank (потому что указали "моно"), с названием "Колесо". Нет, лол, тут контекст в том, что купил "моноколесо", значит счёт – основной, категория покупки, теги техника, название "моноколесо". Чисто из структуры сообщения понятно, что речь идёт не о счёте, а о покупке одного предмета (электроники). 

Внедри это как тонкий post-processing-guard.

Ещё одно: основной счёт пользователя был выбран "Вне Wallet", а он у нас ограничен (он системный, скрытый). Тогда: 
1. "Вне Wallet" не может никогда быть основным счётом. Когда пользователь только регистрируется, у него отсутствует основной счёт. Но, при создании первого счёта – первый созданный счёт должен автоматически становиться основным счётом. Основной счёт можно изменить в настройках (уже реализовано). 
2. Почему-то название всё равно присваивается "Наличные", возможно, из-за того, что валюта фиатная и "вывод" парсится как вывод с карты на руки. Но это неправильно и не релевантно, и мне это не нравится. Как это можно исправить? По логике, запрос "вывел 50 евро" должен быть с названием "Вывод", ведь "Наличные" тут нигде не упоминаются лол. И как это можно исправить? Исправь. 

Ещё дополнительные правки ко всем выше пунктам: 
1. При регистрации Основной счёт: Вне Wallet. Нужно исправить, чтобы Вне Wallet не определялся как основной счёт, а основной счёт был null в БД, отображение в телеграм сообщении как пустое поле. 
2. "Купил кофе за 4 евро" – парсит транзакцию в категорию "Еда и напитки", при этом у пользователя существует категория "Кафе и рестораны" – wtf? Почему не в кафе и рестораны, а в еду и напитки? Ведь кофе обычно покупают в кафе, а не в продуктовых магазинах. Нужно исправить так, чтобы кофейные, блюда и тому подобное – чтобы это добавлялось в более соответствующую категорию. Нельзя хардкодить названия категорий и прочее, ведь у пользователя есть возможность переименовывать категории и создавать новые. Нужно давать общие логические сведения о том, как можно парсить транзакции, по какой логике, и наводить примеры, как будет лучше определить ту или иную транзакцию. Главное не придумывать информацию, а быть релевантным на основе данных пользователя (его категорий). 
3. Заметил маленькую проблему. Хочу, чтобы она была решена (хотя решение не критично) без костылей и без дополнительных созданных проблем: при вводе текста для создания счёта "test 1 uah", ИИ создает счёт с названием "Test 1" и валюту "UAH - 1". То есть единичку он два раза продублировал – в названии и в сумме. Хотя первоначально приоритетно парсится сумма, если уже была взята цифра для суммы – тогда она не входит в название, ведь она уже была взята из текста, и повторно текст не должен браться. При этом нужно ограничить подобное исправление от того, чтобы ИИ не парсил, к примеру, текст "Trust Wallet 1, 100 USDT" как 1100 USDT, потому что единичка была разделена пробелом – а значит это другое число. То есть всё, что разделено пробелом – не является одним и тем же значением (кроме названия). 

Ещё дополнительные задачи 4:
1. Столкнулся с такой проблемой: если хочу удалить какого-то юзера из БД, то постоянно есть с ним связанные элементы, из-за которых я не могу удалить его напрямую. Вопрос: как я могу быстро удалить пользователя, чтобы среди тысячи записей других пользователей (категорий) не искать именно категории этого пользователя? И вообще оцени архитектуру моей БД как многопользовательскую, адекватная ли она или требует улучшений, и каких если да? Жду ответ.
2. Я хочу, чтобы ты внёс исправления по всем 3 архитектурным узким местам, которые ты описал ("Оценка вашей БД как многопользовательской
Адекватная основа для multi-tenant: почти везде есть userId, это правильно. Но есть 3 архитектурных узких места, которые реально аукнутся:

Деньги в Float (Transaction.amount, AccountAsset.amount, курсы/конвертация тоже вокруг float).
Для финансов (особенно crypto) это источник багов по округлениям.
Правильнее: Decimal/NUMERIC в Postgres + единый слой округления/decimals.
Категория в транзакции хранится строкой (Transaction.category: String?)
Это не FK, нет целостности, при переименовании категории исторические транзакции “разъезжаются”, сложно гарантировать корректную аналитику.
Обычно делают categoryId (FK на Category) + опционально “снапшот” имени/эмодзи на момент операции, если нужно.
Каскады/удаление
Для multi-user систем важно уметь атомарно удалить tenant’а (GDPR/право на удаление) без ручного порядка таблиц.
Рекомендация: выровнять onDelete: Cascade в Prisma и реально в БД на всех зависимостях."). Важно: пиши изменения так, как будто ты senior-разработчик. Рефактори код сразу правильно по нужным файлам. Не создавай конфликтов, учитывай весь контекст проекта. После изменений бот должен работать так, как работает сейчас. Твои архитектурные изменения должны быть реально только полезными. 

Обеспечь, чтобы когда юзер удалял свои данные, чтобы можно было распознать его как юзера, который уже использовал Trial версию для бота. Ну, чтобы он не злоупотреблял бесплатными Pro-версиями чисто удаляя свои счета. Кстати, а как это обеспечивается и проверяется? Покупка тарифа происходит через ссылку Stripe на товар по ежемесячной подписке, и там прямо Stripe встроил 7-и дневный пробный период (бесплатный). Что, если пользователь отменит платную подписку после пробного периода, и просто через бота заново создаст ссылку на оплату, и по этой ссылке просто на новую почту снова возьмёт 7-и дневный пробный период? Это так работает, или я ошибаюсь? Нужно ли мне для этого создать новую ссылку-товар на оплату Pro-тарифа, но который не включает в себя пробный период, чтобы для пользователей, которые уже активировали пробную версию (покупали товар по первой ссылке и были Premium) – при повторной попытке купить тариф с пробной версией после отмены старого, чтобы они получали уже другую ссылку на подписку, где нету пробной версии? 

Обеспечь, чтобы я мог удалять пользователя либо сразу в бд через таблицу users, либо кодом через SQL. Прикрепил скриншот, как сейчас выглядит попытка удаления юзера напрямую через users. 

Главная задача: упростить жизнь пользователю и заполнять данные за него максимально релевантно, то есть предугадывать его желания, используя дедукцию и логику. Главное: не усложнять жизнь пользователю и не забирать его время на дополнительные правки нерелевантных данных. 

мне нужен плавный и хороший flow для создания транзакций с типами: расход (покупка в магазине), доход (зарплата например), перевод (ну обычный бакнвоский перевод там кому-то (на Вне Wallet) или между своими счетами). Итого, всю эту архитектуру нужно адаптировать и оптимизировать для любых жизненых случаев, чтобы это работало хорошо и без багов. Проведи со мной интервью, задай вопросы о том, как это можно сделать, точечно, чтобы я выбрал и определил дальнейший путь и значения, а так же задавай вопросы, которые помогут тебе в понимании этого функционала и его разработки. В общем, проведи бриф, и на полученные от меня ответы начнёшь реализацию этого функционала, чтобы всё работало хорошо и оптимизированно. То есть мне важно поставить какие-то ограничения. Я разрешаю минусовые балансы, для fiat я разрешаю конвертацию валют, транзакции расход-доход-перевод нельзя создать с активом, которого нет на счёте, но для этого для fiat и crypto выступает конвертация валют, которая переводит актив которого нету по актуальному курсу на счёт в актив, который есть. Это уже реализовано, и это трогать не нужно (только убедиться, что это работает).

Округлять суммы в отображениях не нужно, суммы сохраняются полные. 

Готовый план для мелких правок: 
## Фикс emoji при rename + контекстная дата vs сумма + базовая pre-prod проверка

### Summary
Нужно закрыть 2 дефекта и сделать лёгкий pre-prod sanity-check:
1. При rename счёта через Jarvis не должен пропадать emoji.
2. Число суммы (`11.1 TON`) не должно становиться датой (`11.01`) и перебивать дату со скриншота.
3. Базово подтвердить, что проект можно пушить/деплоить (без глубокого QA).

### Что уже подтверждено в коде
1. Потеря emoji происходит в ветке rename, где имя нормализуется без гарантии emoji (`normalizeAccountName`), а `updateAccountWithAssets` просто сохраняет `draft.name`.
2. Логика даты уже содержит частичную защиту (`extractExplicitDateFromText` и `shouldIgnoreLlmDateFromAmount`), но она недостаточно жёсткая для кейса “caption с суммой + скрин с датой”.
3. Базовые проверки:
- `npx tsc --noEmit` проходит.
- `npm run build` падает с `EPERM` на очистке `dist` (локальная блокировка/права, не типовая бизнес-ошибка кода).
- `npm run start:prod` запускает приложение, миграции применены, падение только из-за `EADDRINUSE:4200` (порт занят локально).

### План реализации (decision-complete)

1. Фикс emoji при переименовании счёта
- Файл: `src/modules/accounts/accounts.service.ts`
- Добавить централизованный нормализатор имени счёта, который всегда гарантирует emoji в начале.
- Правило сборки имени:
1. Если пользователь в новом имени сам указал emoji первым символом, использовать его как есть.
2. Если в новом имени emoji нет, взять emoji из текущего имени счёта.
3. Если в текущем имени emoji нет, поставить fallback по типу счёта (`bank -> 🏦`, `cash -> 💵`, `crypto -> 🪙`, default `💼`).
4. Очистить хвостовую пунктуацию и лишние пробелы, сохранить текстовую часть имени.
- Применить этот нормализатор внутри `updateAccountWithAssets` перед `tx.account.update`.
- Эффект: любой путь rename (текст, voice, Jarvis details) получает одно поведение и не теряет emoji.
- Дополнительно: в `bot.service.ts` оставить/упростить `normalizeAccountName` как чистку текста, не как источник emoji-правил, чтобы не было дублирующей логики.

2. Жёсткое разделение “сумма” и “дата” в транзакциях
- Файлы:
- `src/modules/bot/bot.service.ts`
- `src/utils/date.ts`
- `src/modules/llm/llm.service.ts`
- Усилить эвристику игнорирования ложной даты:
1. В `shouldIgnoreLlmDateFromAmount` проверять не только `tx.amount`, но и `tradeBaseAmount`, `tradeQuoteAmount`, а также числовые токены рядом с валютами (`TON`, `USDT`, `BTC`, `EUR`, и т.д.).
2. Если `llmDate` совпадает по день/месяц с таким amount-паттерном и явной даты в тексте нет, считать это ложным и сбрасывать `llmDate`.
- Усилить `extractExplicitDateFromText`:
1. `дд.м` без года принимать как дату только при явном date-контексте (например, рядом слова “дата”, “от”, “числа”) или если после токена не идёт валютный/активный хвост.
2. `дд месяц` и `дд.мм.гггг` оставить валидными как явную дату.
- Уточнить приоритет для image-flow в LLM-инструкции:
1. Дата со скриншота приоритетна.
2. Дата из подписи пользователя применяется только если явно задана как дата, а не как amount.
3. Явно запретить трактовать `11.1 TON` как дату.
- Обновить инструкции в `parseTransactionFromImage` и общем `buildTransactionParseInstructions`.

3. Минимальная консистентность после последних изменений
- Проверить, что новые поля/ветки trade не оставляют старых “висячих” путей:
1. Поиск и сверка всех мест `tradeType`, `tradeBase*`, `tradeQuote*`, `executionPrice`, `tradeFee*`.
2. Сверка всех кнопок preview/details, чтобы нет старой ветки `Валюта` для trade.
3. Сверка serialize/deserialize preview-edit-save (`persist-preview-transaction`, `view-transactions`, `confirm/save-delete`).
- Проверка только базовая, без масштабного рефакторинга.

4. Лёгкий pre-prod чек перед push (Railway)
- Выполнить:
1. `npx tsc --noEmit`
2. `npm run build`
3. `npm run start:prod` (короткий smoke запуск)
- Если локально `build` снова даст `EPERM`, зафиксировать как environment issue и дать безопасный workaround:
1. остановить процессы, держащие `dist`,
2. удалить `dist`,
3. повторить build.
- Если `EADDRINUSE`, считать это локальным конфликтом порта, а не блокером кода.
- Отдельно подтвердить:
1. `prestart:prod` выполняет `prisma migrate deploy`,
2. миграции в статусе `No pending migrations`.

### Изменения интерфейсов/типов
1. Публичные API не меняются.
2. Схемы БД не меняются.
3. Внутренний контракт обновления имени счёта в `AccountsService.updateAccountWithAssets` становится строгим: на выходе имя всегда с emoji-префиксом (кроме случая, когда пользователь указал свой emoji).

### Acceptance tests
1. Rename через Jarvis text: было `🪙 Bybit`, ввод `bybit spot` -> сохраняется `🪙 Bybit spot`.
2. Rename через Jarvis voice: без emoji в голосе -> сохраняется прежний emoji.
3. Rename с пользовательским emoji: ввод `🏦 Sparkasse` -> сохраняется `🏦 Sparkasse` (не подменять).
4. Текст: `купил 11.1 TON` -> дата не становится `11.01`; если есть дата со скрина, берётся она.
5. Скрин + caption с amount (`11.1 TON`) + видимая дата на скрине -> в транзакции дата со скрина.
6. Явная дата текстом (`23 февраля` или `23.02.2026`) -> дата берётся из текста.
7. Регрессия: обычные `expense/income/transfer` не меняют поведение.
8. Sanity pre-prod: `tsc` зелёный; `start:prod` поднимает приложение при свободном порте.

### Assumptions / defaults
1. Если пользователь не указал emoji в новом имени, система обязана сохранить предыдущий emoji счёта.
2. Если у счёта исторически нет emoji, используется fallback по `account.type`, иначе `💼`.
3. В image-flow дата со скриншота приоритетнее подписи, кроме случая явной даты в подписи.
4. Кейс `11.1 TON` трактуется как amount-контекст, не как дата.
5. Проверка “готов к пушу” означает базовый технический smoke-check, не полный e2e/regression QA.

Ещё дополнительные правки: 
Что там происходит с системным промптом для LLM-парсера? Почему он такой тупой на распознавание реальных данных? Вот спустя столько правок и улучшений, уже в 10 раз где то улучшаем промпт по понимаю счетов, я пишу ему "11,0924 TON потратил на покупку звёзд в телеграм вчера с байбита", а он создает транзакцию, которой присваивает счёт основной. Какого хуя? Я чётко указал откуда я потратил TON – "с байбита", у меня есть счёт Bybit, на нём есть TON – всё сходится, но он всё равно определяет для основного счёта, в котором нету TON валюты. Еблан. Сюда же категорию неверную сделал: "Покупки", да какие это покупки? Это "Платежи", это же блять платёж а не покупка, еблан. Всё что связано с цифровыми покупками а-дя подписок, звёзд в ТГ и тому подобное – это платежи. Покупки – это всё, что связано с материальным, типа купил ноутбук, заказал вещи. Всё что связано с цифровым (подписки, услуги) – это "Платежи". 

Как это улучшить правильно? Чтобы больше не было подобных ошибок и чтобы решить улучшение парсинга навсегда. Чтобы парсер наконец-то стал реально умным. Чтобы правила не путались между собой и работали слаженно. 

Ещё небольшой план для улучшения парсинга транзакций: 
"План: стабилизация LLM-парсинга
Краткое summary
Исправляем проблему не “ещё одним промптом”, а архитектурно:

LLM остаётся источником черновика, но итог транзакции проходит через детерминированный normalizer (rule-engine).

Если в тексте есть явное упоминание счёта (с байбита) -> Текст > всё.
Для цифровых расходов целевая категория = Платежи, если её нет -> 📦Другое.

Корневые причины (по текущему коду)
Счёт из текста для non-trade почти не подхватывается.
В processParsedTransactions loose text-account match (findLooseMentionedAccount) используется только для trade, из-за этого “с байбита” при expense часто падает в default.
Файл: bot.service.ts (блок назначения matched/matchedAccountId).
Категория digital-платежей не закреплена правилом.
Есть эвристика для food-категорий, но нет детерминированного правила “звёзды/подписки/услуги -> Платежи”.
Файл: bot.service.ts (после parse-merge и перед final category mapping).

1) Rule-engine поверх LLM (основной слой нормализации)
Файл: bot.service.ts
Действия:

Вынести нормализацию в явный конвейер (внутри файла или transaction-normalizer.ts):
normalizeIntentAndDirection(tx, sourceText)
normalizeAccountSelection(tx, sourceText, accounts)
normalizeCategory(tx, sourceText, userCategories)
normalizeTradeConsistency(tx).
Тексты “потратил/оплатил/купил звёзды/подписку/услугу” без биржевых сигналов => expense.
Кейс потратил TON на звёзды не должен превращаться в trade.
Правило account selection (Текст > всё) для всех типов:
Если в исходном тексте есть явное упоминание счёта/алиаса (с байбита, с моно, на нал) — этот счёт выбирается приоритетно, не только для trade.
Если LLM account пустой, всё равно делать findLooseMentionedAccount(sourceText) для non-trade.
Для transfer разделять from/to как сейчас, но использовать text-match для обоих концов.
Усиление account mention:
findLooseMentionedAccount учитывать формы типа с байбита/из байбита (родительный падеж для известных алиасов).
Добавить алиас-формы в accountAliasMap: байбита, байбитa (латинские опечатки), аналогично для часто используемых счетов.
2) Детерминированная категория для digital-платежей
Файл: bot.service.ts
Действия:

Добавить pickDigitalPaymentCategory(sourceText, categoryNames).
Сигналы digital:
звезд, звёзд, telegram stars, подписк, premium, донат, service, услуг, vpn, hosting, domain.
Применение:
Только для non-trade и non-transfer расходов.
Если есть категория, содержащая платеж/платёж -> присвоить её.
Если нет -> 📦Другое (как зафиксировано).

5) Усилить системный промпт, но не полагаться только на него
Файл: llm.service.ts
Действия:

Добавить явные инструкции:
“потратил X TON на звёзды/подписку/услугу -> expense, не trade”.
“если в тексте с/из <счёт> — заполни account этим счётом”.
“digital purchases/services -> категория Платежи (если доступна)”.
Сохранить правило: финальное решение принимает rule-engine в bot слое."

Тесты и сценарии приёмки
Account selection (non-trade):
Ввод: 11,0924 TON потратил на покупку звёзд в телеграм вчера с байбита.
Ожидание: direction=expense, account=🪙 Bybit (если существует), не default из-за отсутствия account в LLM.

Category digital:
Если есть категория Платежи -> выбирается она.
Если нет Платежи -> самая релевантная категория которая есть у пользователя (ибо категории могут быть кастомными) –> если нету никакой подходящей, тогда "📦Другое".

Следующие задания: 
1. Снова таки ошибка. Почему? Мы же улучшила LLM-парсинг пиздец как. Но снова банальная ошибка: пишу запрос "-22,9991 usdt bybit перевод Антону" с чётким словом ПЕРЕВОД. А он создает транзакцию с типом Расход. Ну не еблан ли? Почему так? Почему расход? Почему не перевод? Как сделать так, чтобы определяло максимально релевантно – я сказал что перевёл, значит перевод, а не расход, я же не потратил или не купил. 
"📄 Предпросмотр операции 1/1

🔴 Расход
Антону

Сумма: -22.9991 USDT
Счёт: 🪙 Bybit
Дата: Сегодня
Категория: 📦Другое
Тег: -". Нужно было создать перевод на Вне Wallet (т.к. Антон – человек, это не мой счёт, не мои счета и так далее (за исключением, если бы у меня реально существовал счёт с именем "Антон", тогда перевод был бы на счёт "Антон"), поэтому нужно перевести с Bybit на счёт Вне Wallet (вне)).

Следующее: сейчас отображение сумм в деталях транзакции и в просмотре после создания – округляются. Нужно сделать так, чтобы они не округлялись, а писалось точное число из БД, и без округления. 
2. 2. Почему то когда скидываю скриншот с доп. информацией в текст, например прикрепленный скриншот с текстом "Bybit, 23.02.2026", то бот присылает ошибку "Не удалось распознать сумму операции. Укажите сумму в сообщении и попробуйте снова.". Вероятно, он начинает парсить только текст, и не учитывает изображение, ибо изображение без текста он парсит, но не в нужный счёт. Как сделать так, чтобы он учитывал скриншот, и для доп. информации обращался к входящему тексту? А не только одно из двух. 
3. В детальном просмотре счёта нужно разделить Enterом (то есть сделать отступ) блоки "Активы:" и "Последние операции", чтобы между ними был отступ. 
4. Для аналитики view_analytics нужно "Крупнейший перевод" сделать обычным текстом, а вот его описание (Название + сумма) сделать в форматировании "Цитата" телеграм (<blockquote>). 

План: 
План: Fix transfer-intent, image+caption parsing, exact amount rendering, trade balance invariants
Summary
Цель: убрать 5 наблюдаемых регрессий без костылей, через детерминированную нормализацию после LLM, более устойчивый входной pipeline (photo/document+caption), и строгие инварианты для trade-баланса.

Ключевые причины по текущему коду:

перевод может стать expense, потому что transferHint слишком узкий (с ... на ...), а hasMinusSign перетирает intent.
Предфильтр normalizedWithAmount в processParsedTransactions отбрасывает операции без amount, даже если есть tradeBaseAmount/tradeQuoteAmount.
Нет обработчика message:document для image/*; скрины как файл не идут в vision-парсинг.
В preview/details часть сумм форматируется с округлением (toLocaleString с fixed 2/8), а не как точное значение.
Для account/details и analytics есть точечные несоответствия форматирования.
Important API/Type Changes
Публичные API не меняются.
Внутренние типы:
Добавить helper тип для amount-as-string в рендере (ExactNumericLike) в tx-confirm-msg.ts.
Расширить internal normalize pipeline для transfer-intent.
CLI/API контрактов БД не меняем.
Implementation Plan
Устойчивый intent для перевод (приоритет семантики над знаком)
Файл: bot.service.ts
Вынести/усилить normalizeIntentAndDirection(tx, sourceText):
Если есть слова перевод|перевёл|перевел|отправил|send|sent|перекинул|скинул, считать transfer даже без шаблона с ... на ....
Если есть контрагент-человек (Антону, маме, папе, etc.) и нет сигналов покупки товара/услуги, закреплять transfer.
hasMinusSign больше не должен перетирать уже распознанный transfer-intent.
Для transfer-кейса с одним счётом-источником и контрагентом-человеком:
fromAccount = Bybit (или найденный счёт),
toAccount = Вне Wallet, если нет реального user account с таким именем.
Обновить extractTransferCounterparty для кейса существительных (перевод Антону) и дательного падежа.
Корректный account resolve для transfer-to-person
Файл: bot.service.ts
В блоке account resolution:
Если есть явное упоминание счёта в тексте (с байбита), это всегда источник.
Если toAccount не матчится с пользовательскими счетами, назначать outsideWalletId.
Если существует счёт с именем, совпадающим с получателем (Антон) — использовать его как toAccount.
Fix pipeline amount-gate для image/trade
Файл: bot.service.ts
Исправить normalizedWithAmount:
Не отбрасывать tx, если amount пустой, но присутствует любой из:
tradeBaseAmount,
tradeQuoteAmount,
convertedAmount.
Для trade с пустым amount синтезировать канонический amount из trade-полей до фильтра.
Сообщение об ошибке делать более точным: “не удалось извлечь сумму ни из текста, ни со скриншота”.
Поддержка скринов как document (image/*)
Файл: bot.service.ts
Добавить this.bot.on('message:document', ...):
Если document.mime_type начинается с image/, обрабатывать как message:photo.
Использовать caption как доп. контекст, а изображение как primary source сумм/деталей.
Прокинуть в parseTransactionFromImage те же categoryNames/tags/accounts/memoryHints.
Сохранить текущую логику для message:photo.
Exact amounts в preview/details без округления
Файл: tx-confirm-msg.ts
Заменить локальные форматирования, которые округляют:
convertedStr (сейчас 2 знака) -> exact format.
executionPrice (сейчас до 8 знаков) -> exact format из DB/драфта.
tradeBaseAmount/tradeQuoteAmount -> exact format.
Ввести formatter formatExactAmount(value, currency):
Принимать number|string|Decimal-like.
Для string сохранять дробную часть как есть (без искусственного округления).
Нормализовать только визуально (. -> ,, убрать бесполезные хвостовые нули опционально по флагу).
Не обрезать до fixed decimals.
Проверка trade balance-инварианта (buy/sell)
Файлы: transactions.service.ts, canonicalize-trade.ts
Усилить guard после канонизации:
buy: обязательны quote-debit + base-credit.
sell: обязательны base-debit + quote-credit.
Если tradeType есть, но обе стороны не вычислены, бросать явную ошибку в create/update.
Добавить debug-safe invariant check при apply/reverse effect (в dev/test логах), чтобы ловить расхождения early.
UI spacing в account details
Файл: accounts.ts
Добавить дополнительный перенос строки между Активы: и Последние операции:.
Analytics formatting: “Крупнейший перевод”
Файл: analytics-main.callback.ts
Изменить блок:
Крупнейший перевод: обычным текстом (без <b>).
Линию from → to — сумма оставить в <blockquote>.
Детализацию ниже оставить plain text (как сейчас), если есть.
Test Cases / Acceptance
Transfer intent from plain phrase:
Input: -22,9991 usdt bybit перевод Антону.
Expected: direction=transfer, from=Bybit, to=Вне Wallet, description=Антону.
Transfer to own account override:
Если есть счёт Антон, тот же input -> toAccount=Антон, не Вне Wallet.
Minus sign should not force expense:
Input содержит перевод + -... -> остаётся transfer.
Image + caption:
Скрин с суммой + caption Bybit, 23.02.2026 -> tx создаётся из скрина, caption уточняет счёт/дату.
Не должно быть ошибки “Не удалось распознать сумму...”.
Image as document:
Тот же скрин, отправленный файлом (document image/*) -> тот же результат, что и photo.
Trade parsing without amount but with trade fields:
tx с tradeBaseAmount/tradeQuoteAmount, amount отсутствует -> не отбрасывается prefilter.
Exact amount rendering:
Preview/details показывают точные значения из драфта/БД без fixed rounding.
Buy/sell balance math:
buy: списывает quote, зачисляет base.
sell: списывает base, зачисляет quote.
fee списывается из tradeFeeCurrency.
UI spacing:
В карточке счёта между Активы и Последние операции есть пустая строка.
Analytics transfer block:
Крупнейший перевод: plain.
Строка transfer summary в blockquote.
Assumptions / Defaults
Слово перевод в финансовом контексте приоритетно трактуется как transfer.
Контрагент-человек без matching user account -> Вне Wallet.
Для exact rendering приоритет у исходного decimal/string значения; если доступен только number, показываем максимально точно из него без принудительного fixed округления.
Для image-flow истина по суммам/операциям на стороне изображения, caption — слой уточнений.


Ещё новые исправления: 
1. Транзакция в криптовалюте в детальном просмотре связанного счёта  отображается почему-то в EUR (то есть в основной валюте пользователя), вместо того, чтобы отображаться в своей оригинальной валюте (криптовалюте). Это нужно исправить. 

Все изменения должны быть общими, не хардкодь явные указания только для этого типа действия. Все подобные типы действий должны обрабатываться по логике дедукции, именно поэтому указания нужно делать общими, но чтобы они были рабочими и парсили информацию максимально релевантно и без ошибок. 

Исправь эти нюансы. 

План: отображение валюты в счёте
Исправляем регрессию детерминированно, без хрупких спец-кейсов:

В блоке «Последние операции» счёта переводы показываются в исходной валюте операции, а не только в основной валюте пользователя.
1) Отображение переводов в деталях счёта: исходная валюта + эквивалент
Файл: accounts.ts
Файл: bot.service.ts (формирование lastTransactions)

Решение:

В formatDetailTxLine для tx.direction === 'transfer' рендерить сумму в tx.currency через formatAmount(Math.abs(tx.amount), tx.currency).
В скобках показывать эквивалент в основной валюте: (${fmt(tx.amountMain)} ${mainSym}).
Для transfer использовать знак − (исходящий перевод для выбранного accountId), чтобы не было визуального ощущения «получки».
Точный целевой формат строки:
⚪️ −11,147 TON (12,50 €) | Вывод | Сегодня