---
name: Transaction Preview Editing
overview: Реализация полнофункционального предпросмотра транзакций с пагинацией, редактированием всех полей (тип, название, сумма, счёт, дата, категория) и поддержкой множественных транзакций из одного парсинга.
todos:
  - id: db_schema
    content: "Обновить schema.prisma: модель Category, поле transactionDate, миграция"
    status: completed
  - id: llm_parsing
    content: Обновить LLM сервис для парсинга массива транзакций, счёта и даты
    status: completed
  - id: middleware_session
    content: "Обновить middleware: добавить draftTransactions[], currentIndex, editingField"
    status: completed
  - id: ui_elements
    content: Обновить renderConfirmMessage и confirmKeyboard для отображения всех полей
    status: completed
  - id: edit_type
    content: Создать callback для редактирования типа транзакции
    status: completed
  - id: edit_description
    content: Создать callback для редактирования названия
    status: completed
  - id: edit_amount
    content: Создать callback для редактирования суммы
    status: completed
  - id: edit_account
    content: Создать callback для выбора счёта (с пагинацией)
    status: completed
  - id: edit_date
    content: Создать callback для редактирования даты через LLM
    status: completed
  - id: edit_category
    content: Создать callback для выбора категории (с пагинацией)
    status: completed
  - id: save_delete
    content: Обновить callbacks для сохранения и удаления 1/всех транзакций
    status: completed
  - id: pagination
    content: Создать callbacks для пагинации транзакций в предпросмотре
    status: completed
  - id: repeat_close
    content: Создать callbacks для кнопок Повторить и Закрыть
    status: completed
  - id: services
    content: Обновить transactions/accounts/categories сервисы
    status: completed
  - id: bot_handler
    content: Обновить bot.service.ts для обработки массива транзакций
    status: completed
  - id: utils
    content: Создать утилиты для форматирования суммы и даты
    status: completed
isProject: false
---

# Реализация редактирования транзакций в предпросмотре

## 1. База данных

### Миграция schema.prisma

Добавить модель Category:

```prisma
model Category {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  name      String
  createdAt DateTime @default(now())
  
  @@map("categories")
}
```

Добавить поле transactionDate в Transaction:

```prisma
transactionDate DateTime @default(now())
```

Обновить User model, добавить связь:

```prisma
categories Category[]
```

### Дефолтные категории

Создать при регистрации пользователя: Продукты, Транспорт, Развлечения, Здоровье, Кафе, Подписки, Одежда, Зарплата, Подарки, Связь, Доход, Комиссии, Штрафы, Услуги, Коммунальные платежи, Сервисы, Покупки, Жилье, Уход за собой, Спорт, Путешествия, Трейдинг, Спот

## 2. LLM парсинг (множественные транзакции)

### [llm.service.ts](c:\Users\losad\Desktop\alex-tg-agent\src\modules\llm\llm.service.ts)

- Изменить возврат с объекта на массив транзакций
- Добавить парсинг счёта (account name) из текста
- Добавить парсинг даты (transactionDate)
- Обновить system prompt для возврата массива

### [transaction.schema.ts](c:\Users\losad\Desktop\alex-tg-agent\src\modules\llm\schemas\transaction.schema.ts)

```typescript
LlmTransactionSchema = z.array(z.object({
  // поля транзакции
  account: z.string().optional(), // название счёта
  transactionDate: z.string().optional() // дата в любом формате
}))
```

## 3. Session & Middleware

### [bot.middleware.ts](c:\Users\losad\Desktop\alex-tg-agent\src\modules\bot\core\bot.middleware.ts)

Обновить session:

```typescript
session: {
  draftTransactions?: LlmTransaction[] // массив транзакций
  currentTransactionIndex: number // текущая страница в предпросмотре
  editingField?: 'type' | 'description' | 'amount' | 'account' | 'date' | 'category'
  editingTransactionId?: string // id транзакции в массиве
  editAccountPage?: number // страница счетов
  editCategoryPage?: number // страница категорий
  // остальные поля
}
```

## 4. UI элементы

### [tx-confirm-msg.ts](c:\Users\losad\Desktop\alex-tg-agent\src\modules\bot\elements\tx-confirm-msg.ts)

Обновить renderConfirmMessage:

- Отображать тип транзакции (📉 Расход / 📈 Доход / 🔄 Перевод)
- Для transfer показывать "Откуда → Куда"
- Форматировать сумму: "1 234.56 €"
- Показывать название счёта из Account
- Форматировать дату: "Сегодня" / "Вчера" / "ДД.ММ.ГГГГ", время всегда текущее (из Telegram user's time если пользователь его явно не указывает, даже если дата уже прошедшая – время соответствует текущему времени пользователя).
- Показывать название категории

### [confirm-tx.ts](c:\Users\losad\Desktop\alex-tg-agent\src\modules\bot\callbacks\confirm-tx.ts)

Обновить confirmKeyboard:

- Показывать кнопки пагинации (« Назад, 1/N, Вперёд ») только если транзакций >1
- Добавить кнопку "Повторить" в последнем ряду

## 5. Callbacks для редактирования

Создать файлы в `src/modules/bot/callbacks/`:

### edit-type.callback.ts

- Показать inline-кнопки: "Расход", "Доход", "Перевод"
- Отметить текущий тип галочкой (✓)
- После выбора обновить тип в draftTransactions[currentIndex]
- Удалить сообщение выбора типа
- Обновить предпросмотр через editMessageText
- Кнопка "Закрыть" в последнем ряду

### edit-description.callback.ts

- Отправить сообщение-подсказку "Введите новое название"
- Установить editingField = 'description'
- В обработчике message:text обработать ввод
- Capitalize первую букву
- Удалить сообщение-подсказку и сообщение пользователя
- Обновить предпросмотр
- Кнопка "Закрыть"

### edit-amount.callback.ts

- Подсказка "Введите новую сумму (только число)"
- Установить editingField = 'amount'
- Валидация: только number
- При ошибке показать "Некорректная сумма, попробуйте снова"
- Форматирование: "1 234.56 €"
- Кнопка "Закрыть"

### edit-account.callback.ts

- Получить все счета пользователя из БД
- Показать в 3 columns, 5 рядов:
  - Ряды 1-3: кнопки счетов (по 3 в ряд)
  - Ряд 4: (« Назад) (1/N) (Вперёд ») если >9 счетов
  - Ряд 5: (Отменить)
- При выборе счёта обновить accountId в draftTransactions
- Для transfer показывать выбор 2 счетов: сначала fromAccount, потом toAccount
- Обновить предпросмотр

### edit-date.callback.ts

- Подсказка "Введите дату: Сегодня, Вчера или конкретную дату"
- Установить editingField = 'date'
- Передать текст в LLM для парсинга даты
- При ошибке парсинга: "Не удалось распознать дату"
- Обновить transactionDate
- Кнопка "Закрыть"

### edit-category.callback.ts

- Получить категории пользователя из БД
- Показать в 3 columns, 5 рядов (как со счетами)
- Пагинация если >9
- При выборе обновить categoryId
- Обновить предпросмотр
- Кнопка "Отменить"

### save-transaction.callback.ts

Для confirm_1_transactions:

- Получить текущую транзакцию из draftTransactions[currentIndex]
- Сохранить в БД через transactionsService.create
- Удалить из массива draftTransactions
- Если массив пустой - закрыть предпросмотр, показать success
- Если остались транзакции - показать следующую, обновить пагинацию

Для confirm_tx (сохранить все):

- Сохранить все транзакции из draftTransactions
- Очистить массив
- Показать success

### delete-transaction.callback.ts

Для cancel_1_transactions:

- Удалить draftTransactions[currentIndex]
- Обновить пагинацию
- Показать следующую или закрыть если пустой массив

Для cancel_tx (удалить все):

- Очистить draftTransactions
- Закрыть предпросмотр
- Вернуться в home

### pagination-transactions.callback.ts

- pagination_back_transactions: currentIndex--
- pagination_forward_transactions: currentIndex++
- pagination_preview_transactions: показать "N/M" (noop callback)
- Обновить сообщение через editMessageText

### close-edit.callback.ts

- Универсальный callback 'close_edit'
- Удалить текущее сообщение редактирования
- Сбросить editingField
- Оставить предпросмотр активным

### repeat-parse.callback.ts

- Callback 'repeat_parse'
- Активировать awaitingTransaction = true
- Показать сообщение-подсказку о парсинге (как в add-transaction)
- Кнопка "Закрыть"

## 6. Обновление bot.service.ts

### message:text handler

Обработать множественные транзакции:

```typescript
if (ctx.session.awaitingTransaction) {
  const parsed = await llmService.parseTransaction(text) // теперь возвращает массив
  
  ctx.session.draftTransactions = parsed
  ctx.session.currentTransactionIndex = 0
  ctx.session.confirmingTransaction = true
  
  // показать первую транзакцию
  const msg = await ctx.reply(
    renderConfirmMessage(parsed[0]), 
    { reply_markup: confirmKeyboard(parsed.length) }
  )
}
```

Обработать editingField для каждого поля (description, amount, date, category).

## 7. Сервисы

### [transactions.service.ts](c:\Users\losad\Desktop\alex-tg-agent\src\modules\transactions\transactions.service.ts)

- Добавить createMany для сохранения массива транзакций
- При transfer обновлять fromAccount и toAccount
- Добавить transactionDate в create

### [accounts.service.ts](c:\Users\losad\Desktop\alex-tg-agent\src\modules\accounts\accounts.service.ts)

- Добавить метод getAllByUserId для получения всех счетов пользователя
- Добавить метод findByName для поиска счёта по названию (для LLM парсинга)

### categories.service.ts (новый)

- Создать CategoriesService
- Метод createDefaults(userId) - создать дефолтные категории
- Метод getAllByUserId(userId) - получить все категории пользователя
- Вызвать createDefaults при создании пользователя в users.service

## 8. Утилиты

Создать `src/utils/format.ts`:

```typescript
function formatAmount(amount: number, currency: string): string {
  // "1 234.56 €"
  const formatted = amount.toLocaleString('ru-RU', {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
  })
  return `${formatted} ${getCurrencySymbol(currency)}`
}

function getCurrencySymbol(currency: string): string {
  const map = { EUR: '€', USD: '$', UAH: '₴', RUB: '₽' }
  return map[currency] || currency
}
```

Создать `src/utils/date.ts`:

```typescript
function formatTransactionDate(date: Date): string {
  const today = new Date()
  const yesterday = new Date(today)
  yesterday.setDate(today.getDate() - 1)
  
  if (isSameDay(date, today)) return 'Сегодня'
  if (isSameDay(date, yesterday)) return 'Вчера'
  
  return date.toLocaleDateString('ru-RU')
}
```

## Архитектурные решения

1. **Множественные транзакции**: хранятся в `session.draftTransactions` как массив, пользователь переключается через пагинацию
2. **Редактирование полей**: каждое поле имеет свой callback и состояние в `editingField`
3. **Закрытие редактирования**: универсальная кнопка "Закрыть" с callback `close_edit` для всех сообщений
4. **Категории**: отдельная модель в БД с дефолтными значениями при регистрации
5. **Балансы**: динамический расчёт через aggregate (как сейчас)
6. **Transfer**: при выборе типа "Перевод" кнопка "Счёт" открывает последовательный выбор 2 счетов
7. **LLM**: парсит массив транзакций + счёт + дату из текста

## Важные детали

- Все сообщения-подсказки содержат кнопку "Закрыть"
- При редактировании удалять старые сообщения (подсказки + ввод пользователя)
- Пагинация показывается только если транзакций >1
- Форматирование суммы всегда с пробелами и 2 знаками после запятой
- Первое слово названия всегда с заглавной буквы
- Дата может быть в любом формате, парсится через LLM
- Счета и категории с пагинацией при >9 элементах

